import { inputText } from './inputText.'
import { radioButton } from './radioButton'
import { rate } from './rate'

const formComponents: { [index: string]: any } = {
    inputText,
    radioButton,
    rate,
}

export default formComponents
